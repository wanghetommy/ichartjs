{
  "name": "ichartjs",
  "version": "2.0.0-rc.1",
  "description": "ichartjs project with the 2.0 Agent-first core",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/index.mjs && node --check src/charts.mjs && node --check src/renderer.mjs && node --check src/edit-controller.mjs",
    "rc:check": "npm test && npm run check && git diff --check"
  },
  "author": "taylor",
  "license": "apache license 2.0"
}
